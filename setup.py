from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Semantic layer for facet planner cube',
    author='rando',
    license='MIT',
)
